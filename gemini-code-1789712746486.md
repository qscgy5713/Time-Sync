# 專案架構書：免登入時間對齊工具 (Time-Sync)

## 1. 專案概述
本系統旨在解決多人協作、跨部門會議或聚餐時「時間難以對齊」的溝通痛點。主打**免註冊、免登入、極簡操作**。發起人建立候選時間後，參與者透過專屬連結即可畫出自己有空的時段，系統會自動計算並以視覺化熱區（Heatmap）呈現最大交集。

## 2. 技術選型 (Tech Stack)

*   **前端介面 (Client-Side)**：Vue.js 3 (Composition API) + Vite
*   **UI 框架**：Tailwind CSS (客製化極簡介面)
*   **狀態管理**：Pinia 
*   **後端服務 (Server-Side)**：Go (Golang - Gin/Fiber) 或 PHP (Laravel) 
*   **關聯式資料庫 (Database)**：PostgreSQL (利用其對時區的嚴謹處理)
*   **部署架構**：容器化 (Docker)，可無縫部署至雲端環境 (如 GKE)。

## 3. 資料庫設計 (Schema)

核心資料結構去蕪存菁，僅需 4 張資料表即可完成完整邏輯。建議外部查詢的 Primary Key 採用 UUID，防止流水號遭猜測。

### `events` (活動主表)
| 欄位名稱 | 型態 | 屬性 | 說明 |
| :--- | :--- | :--- | :--- |
| `id` | UUID | Primary Key | 系統生成的唯一活動碼，用於 URL |
| `title` | Varchar(100) | Not Null | 活動名稱 |
| `description` | Text | Nullable | 活動補充說明 |
| `timezone` | Varchar(50) | Not Null | 發起人所在時區 (如 `Asia/Taipei`) |
| `created_at` | Timestamp | Not Null | 建立時間 |

### `event_options` (發起人提供的時段選項)
| 欄位名稱 | 型態 | 屬性 | 說明 |
| :--- | :--- | :--- | :--- |
| `id` | BigInt | Primary Key | 選項流水號 |
| `event_id` | UUID | Foreign Key | 關聯至 `events.id` |
| `start_datetime` | Timestamp | Not Null | 該區塊的開始時間 (UTC) |
| `end_datetime` | Timestamp | Not Null | 該區塊的結束時間 (UTC) |

### `participants` (參與者表)
| 欄位名稱 | 型態 | 屬性 | 說明 |
| :--- | :--- | :--- | :--- |
| `id` | BigInt | Primary Key | 參與者流水號 |
| `event_id` | UUID | Foreign Key | 關聯至 `events.id` |
| `name` | Varchar(50) | Not Null | 參與者填寫的顯示名稱 |
| `created_at` | Timestamp | Not Null | 填寫時間 |

### `availabilities` (參與者有空的時段)
| 欄位名稱 | 型態 | 屬性 | 說明 |
| :--- | :--- | :--- | :--- |
| `participant_id` | BigInt | Foreign Key | 關聯至 `participants.id` |
| `event_option_id`| BigInt | Foreign Key | 關聯至 `event_options.id` |
*(註：本表 Primary Key 設定為 `participant_id` 與 `event_option_id` 的 Composite Key)*

---

## 4. API 規格定義 (RESTful)

### 4.1 建立新活動
*   **Endpoint**: `POST /api/events`
*   **Request Payload**:
    ```json
    {
      "title": "Q3 開發進度同步會議",
      "description": "請圈選下週有空的時段",
      "timezone": "Asia/Taipei",
      "options": [
        { "start_datetime": "2026-09-21T02:00:00Z", "end_datetime": "2026-09-21T03:00:00Z" },
        { "start_datetime": "2026-09-22T05:00:00Z", "end_datetime": "2026-09-22T06:00:00Z" }
      ]
    }
    ```
*   **Response**: `201 Created` 回傳包含 UUID 的活動網址。

### 4.2 取得活動詳情與目前統計
*   **Endpoint**: `GET /api/events/{event_id}`
*   **Response**: `200 OK` 回傳活動基礎資訊、所有選項清單，以及已填寫的參與者與其勾選的選項 ID。

### 4.3 送出參與者時間
*   **Endpoint**: `POST /api/events/{event_id}/participants`
*   **Request Payload**:
    ```json
    {
      "name": "ianwang",
      "available_option_ids": [101, 102, 105]
    }
    ```
*   **Response**: `201 Created` 

---

## 5. 核心演算法與前端處理邏輯

### 5.1 跨時區轉換 (Timezone Handling)
後端資料庫 (`start_datetime`, `end_datetime`) 統一以 **UTC+0** 儲存。
前端取得資料後，利用 JavaScript 的 `Intl.DateTimeFormat` 自動偵測參與者瀏覽器的本地時區，將 UTC 時間轉換為參與者的當地時間顯示，實現無縫的跨國會議排程。

### 5.2 視覺化熱區渲染 (Heatmap Calculation)
交集運算完全交由前端 Vue.js 處理，降低後端效能負擔。
1.  **資料聚合**：將 API 回傳的 `availabilities` 資料進行 Map/Reduce，計算每個 `event_option_id` 獲得的票數。
2.  **動態樣式綁定**：
    設定總參與人數為 $N$，某時段得票數為 $X$。
    *   $X = N$ (全員到齊)：區塊套用 `bg-green-600 text-white` (深綠)。
    *   $N/2 \le X < N$ (過半數有空)：區塊套用 `bg-green-300 text-gray-800` (淺綠)。
    *   $X = 0$ (無人有空)：區塊套用 `bg-gray-100 text-gray-400` (淺灰)。

### 5.3 拖拉選取實作 (Drag-to-Select)
利用 Vue 響應式變數記錄滑鼠狀態 (`isMouseDown`)。在每個時間區塊綁定 `@mousedown`, `@mouseenter` 與 `@mouseup` 事件，當滑鼠按下並移動經過相鄰的時間區塊時，動態將該區塊的 ID 加入或移出選取陣列（Pinia Store），達到類似 Google Calendar 的流暢圈選體驗。